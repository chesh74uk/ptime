<%= simple_form_for @test do |t| %>
            <%= t.input :test_customer, collection: @customers %>
            <%= t.input :test_scenario, collection: @scenarios %>
            <%= t.button :submit, class: "btn btn-primary" %>
<% end %>